This is a Python package to handle ZIO devices. It includes varius python's
modules which describe all the ZIO's objects:

- Base: is an abstract class for all ZIO objects
- Device: it describes the zio_device structure of a ZIO device
- Cset: it describes the zio_cset structure of a ZIO device
- Channel: it describes the zio_channel structure of a ZIO device
- Buffer: it describes the zio_bi struture of a ZIO buffer
- Trigger: it describes the zio_ti structure of a ZIO trigger
